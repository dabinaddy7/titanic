# 📝 마크다운(Markdown) 핵심 문법 요약 총정리

## 1. 🏗️ 제목 (글자 크기)
`#` 개수가 적을수록 크고 중요한 제목입니다. (뒤에 한 칸 띄어쓰기 필수!)
```markdown
# 1단계 제일 큰 제목 (문서 제목)
## 2단계 중간 제목 (큰 주제)
### 3단계 작은 제목 (소주제)
```

---

## 2. 📋 목록 (리스트)
`*`나 `-`를 쓰고 한 칸 띄우면 점 목록이 됩니다. 하위 목록은 **Tab**키로 들여씁니다.
```markdown
* 상위 항목
  * 하위 항목 (Tab 치고 입력)
1. 숫자 목록은
2. 그냥 숫자로 입력
```

---

## 3. ✍️ 글자 강조
```markdown
**굵게 강조할 텍스트**
*기울임(이탤릭) 텍스트*
~~취소선 텍스트~~
```

---

## 4. 💻 코드 블록 (개발자 필수)
* **본문 중간 (인라인):** 백틱 1개 `` `코드` `` 로 감싸기
* **여러 줄 (블록):** 백틱 3개 후 언어 이름(python, sql 등) 지정
```markdown
이 함수는 `pd.read_csv()` 입니다.

```python
import pandas as pd
df = pd.read_csv('data.csv')
```
```

---

## 5. 💡 인용구 & 구분선
```markdown
> 중요한 팁이나 참고사항은 앞에 꼭표(>)를 붙입니다.

--- (화면을 가로지르는 깔끔한 회색 구분선)
```

---

## 🔗 6. 링크 & 이미지
```markdown
[클릭할 글자](인터넷 주소)
![이미지 이름](이미지 파일 경로 또는 주소)
```